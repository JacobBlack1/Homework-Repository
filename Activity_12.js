// Creates an array containing names of three students in the class  
var students = ("Jacob", "Iris", "Sean")

// Logs length of the students array
console.log(names.length);

// Now, write a console log introducing each student
console.log("welcome to the class " + names[0] + ".");
console.log("welcome to the class " + names[1] + ".");
console.log("welcome to the class " + names[2] + ".");

// Replace the first student in the array with a new student Zion
student[0] = "Zion";

// Use your Java =script to check if "Zion" is the first element in th array
if (student(0) === "Zion") {
    console.log(students[0] + " is in class!");
}
